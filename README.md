# UBS Analyzer (Vite + React)

## Local
```bash
npm install
npm run dev
```

## GitHub Pages
1. Remplace `base: "/UBS/"` dans `vite.config.js` par `"/<NOM_DU_REPO>/"`
2. Push sur `main`
3. GitHub → Settings → Pages → Source = **GitHub Actions**
